export interface SectionMeta {
  title: string;
  subtitle: string;
  description: string;
  slug: string;
}
export interface Section {
  name: string;
  description: string;
  icon: {
    url: string;
  };
}
